# The console's ONE config value: which socket it plugs into.
# NEXT_PUBLIC_ = inlined into the browser bundle at BUILD time.
# A URL is publishable. A key never is — that's why this app has no other vars.
NEXT_PUBLIC_SCOUT_URL=
